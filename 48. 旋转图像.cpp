#include <bits/stdc++.h>
using namespace std;
class Solution
{
public:
    void rotate(vector<vector<int>> &matrix)
    {
        // 先想如何映射 把他拉直
    }
    int mapping(vector<vector<int>> &matrix, int i, int j, int n, int ring)
    {
        int index = i + n - 1;
        if (i > n)
            return;
    }
};